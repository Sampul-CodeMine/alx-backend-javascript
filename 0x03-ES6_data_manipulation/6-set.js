#!/usr/bin/env node

/**
 * This is a function to create a set from an array
 * @param {Array} arr - Source array
 * @returns a set
 */

export default function serFromArray(arr) {
  return new Set(arr);
}
